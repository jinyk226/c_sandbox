### Change output location

`cc <file_path> -o <output-path>`

### Notes

EOF is hit automatically at the end of the file. When reading from the terminal interactively, you always need to signal it manually via Ctrl+D (Unix/Mac) or Ctrl+Z (Windows)
